% Hyperref — PDF links and metadata
% Metadata values are set in .pxis/preset.tex by workspace sync
\usepackage[
  hidelinks,
  breaklinks=true,
  pdfusetitle
]{hyperref}

% Colors applied after \hypersetup in preset.tex
